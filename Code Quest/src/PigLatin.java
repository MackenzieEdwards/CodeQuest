import java.util.Scanner;

public class PigLatin
	{

	public static void main(String[] args)
		{
		for(int i = 0; i < txtfile.length -1; i++;)
			{
			if(txtfile.charAt(0).equals("a") || txtfile.charAt(0).equals("e") || txtfile.charAt(0).equals("i") || txtfile.charAt(0).equals("o") || txtfile.charAt(0).equals("u"))
				{
				System.out.println(txtfile + "yay");
				}
			if()
			}
		String original = null;
		String translated;
		char firstLetter;

		// translated = original;

		
		firstLetter = original.charAt(0);

		translated = original.substring(1) + firstLetter + "ay";


		}
	}
